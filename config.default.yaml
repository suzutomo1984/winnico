# WinNico デフォルト設定
# このファイルをコピーして config.yaml を作り、自分用にカスタマイズしてください
# cp config.default.yaml config.yaml

# キャラクター画像のパス（winnico フォルダからの相対パス、または絶対パス）
character_image: "character.png"

# フォーカス対象ウィンドウのタイトルキーワード（部分一致、大文字小文字無視）
# 複数指定した場合は上から順に探して最初にヒットしたウィンドウをフォーカスする
# 注意: "Claude" はブラウザで Claude.ai を開いているタブにも一致する場合があります
# 例: "Cursor", "Visual Studio Code", "Antigravity", "Windows PowerShell"
target_window_titles:
  - "Terminal"

# チャット入力欄のクリック位置（ウィンドウ下端からの距離 px）
# エディタによって調整が必要な場合は変更する
chat_input_offset_from_bottom: 60

# マウスカーソルを動かしてクリックする方式（デフォルト: false）
# false: SendMessage でクリックイベントを送信（カーソルが動かない）
# true:  SetCursorPos + mouse_event で物理クリック（カーソルが動く）
# Electron系エディタ（Cursor, VSCode等）でフォーカスが効かない場合は true にする
use_cursor_pos: false
